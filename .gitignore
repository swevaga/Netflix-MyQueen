# Dependencies (Tailwind/PostCSS) — install ulang via `npm install`
node_modules/

# OS / editor
.DS_Store
Thumbs.db

# File sisa FUSE (muncul saat file diganti/di-rename selagi dibuka proses lain)
.fuse_hidden*

# Logs
*.log

# Environment variables lokal (berisi secret) — JANGAN commit.
# Template aman (tanpa secret) boleh di-commit: .env.example
.env
.env.*
!.env.example

# Playwright artifacts
playwright-report/
test-results/

# Backup data saat pemeliharaan
.backup-data/
